const path = require('path')

module.exports = {
    config: path.resolve(__dirname, 'src', 'config', 'database.js')
}
//passando os caminho do arquivo de configurações/credenciais do BD para o sequelize-cli para rodarmos códigos no terminal